# Kahn-s-Algorithm
Kahn’s Algorithm is a method used to perform Topological Sorting of a Directed Acyclic Graph (DAG) using Breadth-First Search (BFS) by repeatedly removing nodes with zero indegree (nodes that have no incoming edges).
